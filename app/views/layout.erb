<!DOCTYPE html>
<html lang="en">
<head>
  <!--
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>
  <title>Flashcards</title>

</head>
<body>
  <header role="banner" class="mar-t-5 pad-t-2 pad-b-4 pad-s-1 wrap-float bg-white">
    <h1>Flashcards</h1>
    <div class="max-w-700 center wrap-float">
      <nav class="clearfix mar-b-1">
        <ul class="nav-style no-bullets no-margin no-padding right">
          <% if !logged_in? %>
            <li class="pipe-separate t-light-green left"><a href="/users/new">register</a> | <a href="/users/login">login</a> | <a href="/">home</a></li>
          <% else %>
            <% user = user_session %>
            <li class="pipe-separate t-light-green left"><a href="/users/<%= user.id %>"><%=user.username%></a> | <a href="/users/logout">logout</a> | <a href="/">home</a></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </header>
  <%= yield %>
</body>
</html>
