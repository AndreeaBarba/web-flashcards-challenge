<h1><%=@round.deck.title%></h1>

<p>You've completed a round of <%=@round.deck.title%>. You answered <%= @round.guesses.where(is_first_try: true).count%> / <%=@round.guesses.count%> cards correctly on the first try. You submitted <%=@round.guess_count%> total guesses before each card was answered correctly.</p>
